apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prysm-monitoring.fullname" . }}-test-connection"
  labels:
    {{- include "prysm-monitoring.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prysm-monitoring.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
